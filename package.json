{
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "latest",
    "dotenv-cli": "latest",
    "eslint": "^10.0.0",
    "turbo": "latest",
    "typescript": "^6.0.0",
    "vitest": "latest"
  }
}
