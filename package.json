{
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "smoke": "dotenv -e .env.test -- vitest run apps/sandbox/tests",
    "lint": "turbo run lint",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@bobbykim/manguito-cms-core": "workspace:*",
    "@bobbykim/manguito-cms-db": "workspace:*",
    "@bobbykim/manguito-cms-test-utils": "workspace:*",
    "@changesets/cli": "latest",
    "dotenv-cli": "latest",
    "drizzle-orm": "^0.45.0",
    "eslint": "^10.0.0",
    "turbo": "latest",
    "typescript": "^6.0.0",
    "vitest": "latest"
  }
}
