// manguito validate — parse and validate all schemas, config, roles, routes
import type { Command } from 'commander'

export function registerValidate(_program: Command): void {
  // TODO: implement
}
