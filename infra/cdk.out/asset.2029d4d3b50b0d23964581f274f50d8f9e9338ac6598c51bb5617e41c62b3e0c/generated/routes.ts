// Auto-generated by generateRoutes() — do not hand-edit
import { createRoute, z } from '@hono/zod-openapi'

// ─── Shared schemas ──────────────────────────────────────────────────────────

const ErrorResponseSchema = z.object({
  ok: z.literal(false),
  error: z.object({
    code: z.string(),
    message: z.string(),
    details: z.unknown().optional(),
  }),
})

function listResponseSchema<T extends z.ZodTypeAny>(dataSchema: T) {
  return z.object({
    ok: z.literal(true),
    data: z.array(dataSchema),
    meta: z.object({
      total: z.number().int(),
      page: z.number().int(),
      per_page: z.number().int(),
      total_pages: z.number().int(),
      has_next: z.boolean(),
      has_prev: z.boolean(),
    }),
  })
}

function itemResponseSchema<T extends z.ZodTypeAny>(dataSchema: T) {
  return z.object({
    ok: z.literal(true),
    data: dataSchema,
  })
}

// ─── Paragraph schemas ───────────────────────────────────────────────────────

const CardImageLinkSchema = z.object({ card_image_link_title: z.string(), card_image_link_image: z.object({ id: z.string().uuid(), url: z.string().url(), mime_type: z.string(), alt: z.string().optional(), file_size: z.number().int(), width: z.number().int().optional(), height: z.number().int().optional() }), card_image_link_text: z.string().optional(), card_image_link_link: z.object({ link_item_url: z.string(), link_item_target: z.enum(["_self", "_blank"]), link_item_text: z.string() }) })
const LinkItemSchema = z.object({ link_item_url: z.string(), link_item_target: z.enum(["_self", "_blank"]), link_item_text: z.string() })
const PhotoCardSchema = z.object({ photo_card_title: z.string(), photo_card_image: z.object({ id: z.string().uuid(), url: z.string().url(), mime_type: z.string(), alt: z.string().optional(), file_size: z.number().int(), width: z.number().int().optional(), height: z.number().int().optional() }), photo_card_text: z.string().optional() })

// ─── Content type schemas ────────────────────────────────────────────────────

const BlogPostSchema = z.object({
  id: z.string().uuid(),
  slug: z.string(),
  published: z.boolean(),
  created_at: z.string().datetime(),
  updated_at: z.string().datetime(),
  blog_title: z.string(),
  blog_hero_image: z.object({ id: z.string().uuid(), url: z.string().url(), mime_type: z.string(), alt: z.string().optional(), file_size: z.number().int(), width: z.number().int().optional(), height: z.number().int().optional() }).optional(),
  blog_desc: z.string(),
  blog_meta_title: z.string(),
  blog_meta_desc: z.string(),
  blog_cards: z.array(z.object({ photo_card_title: z.string(), photo_card_image: z.object({ id: z.string().uuid(), url: z.string().url(), mime_type: z.string(), alt: z.string().optional(), file_size: z.number().int(), width: z.number().int().optional(), height: z.number().int().optional() }), photo_card_text: z.string().optional() })),
  blog_link: z.object({ link_item_url: z.string(), link_item_target: z.enum(["_self", "_blank"]), link_item_text: z.string() }).optional(),
  blog_related: z.string().uuid().optional(),
})

const ExamplePageSchema = z.object({
  id: z.string().uuid(),
  slug: z.string(),
  published: z.boolean(),
  created_at: z.string().datetime(),
  updated_at: z.string().datetime(),
  title: z.string(),
  hero_image: z.object({ id: z.string().uuid(), url: z.string().url(), mime_type: z.string(), alt: z.string().optional(), file_size: z.number().int(), width: z.number().int().optional(), height: z.number().int().optional() }).optional(),
  body: z.string().optional(),
})

// ─── Taxonomy type schemas ───────────────────────────────────────────────────

const DailyPostSchema = z.object({
  id: z.string().uuid(),
  published: z.boolean(),
  created_at: z.string().datetime(),
  updated_at: z.string().datetime(),
  daily_title: z.string(),
  daily_desc: z.string(),
})

// ─── Content routes ─────────────────────────────────────────────────────────

export const getBlogPostListRoute = createRoute({
  method: 'get',
  path: '/api/blog-post',
  tags: ["Blog Post"],
  request: {
    query: z.object({
    page: z.coerce.number().int().min(1).default(1),
    per_page: z.coerce.number().int().min(1).max(100).default(10),
    include: z.string().optional().describe('Comma-separated relation field names to expand'),
    sort_by: z.enum(['title', 'created_at', 'updated_at']).optional().default('created_at'),
    sort_order: z.enum(['asc', 'desc']).optional().default('asc'),
  }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: listResponseSchema(BlogPostSchema) } },
      description: 'List of Blog Post',
    },
    400: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Invalid query parameters',
    },
  },
})

export const getBlogPostRoute = createRoute({
  method: 'get',
  path: '/api/blog-post/{slug}',
  tags: ["Blog Post"],
  request: {
    params: z.object({ slug: z.string() }),
    query: z.object({ include: z.string().optional().describe('Comma-separated relation field names to expand') }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: itemResponseSchema(BlogPostSchema) } },
      description: 'Blog Post',
    },
    404: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Blog Post not found',
    },
  },
})

export const adminGetBlogPostListRoute = createRoute({
  method: 'get',
  path: '/admin/api/blog-post',
  tags: ["Blog Post"],
  request: {
    query: z.object({
    page: z.coerce.number().int().min(1).default(1),
    per_page: z.coerce.number().int().min(1).max(100).default(10),
    include: z.string().optional().describe('Comma-separated relation field names to expand'),
    sort_by: z.enum(['title', 'created_at', 'updated_at']).optional().default('created_at'),
    sort_order: z.enum(['asc', 'desc']).optional().default('asc'),
    published: z.enum(['true', 'false']).optional().describe('Filter by published state'),
  }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: listResponseSchema(BlogPostSchema) } },
      description: 'List of Blog Post',
    },
    400: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Invalid query parameters',
    },
  },
})

export const adminGetBlogPostRoute = createRoute({
  method: 'get',
  path: '/admin/api/blog-post/{id}',
  tags: ["Blog Post"],
  request: {
    params: z.object({ id: z.string().uuid() }),
    query: z.object({ include: z.string().optional().describe('Comma-separated relation field names to expand') }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: itemResponseSchema(BlogPostSchema) } },
      description: 'Blog Post',
    },
    404: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Blog Post not found',
    },
  },
})

export const adminCreateBlogPostRoute = createRoute({
  method: 'post',
  path: '/admin/api/blog-post',
  tags: ["Blog Post"],
  request: {
    body: {
      content: { 'application/json': { schema: BlogPostSchema } },
    },
  },
  responses: {
    201: {
      content: { 'application/json': { schema: itemResponseSchema(BlogPostSchema) } },
      description: 'Blog Post created',
    },
    422: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Validation error',
    },
  },
})

export const adminUpdateBlogPostRoute = createRoute({
  method: 'patch',
  path: '/admin/api/blog-post/{id}',
  tags: ["Blog Post"],
  request: {
    params: z.object({ id: z.string().uuid() }),
    body: {
      content: { 'application/json': { schema: BlogPostSchema.partial().extend({ published: z.boolean().optional() }) } },
    },
  },
  responses: {
    200: {
      content: { 'application/json': { schema: itemResponseSchema(BlogPostSchema) } },
      description: 'Blog Post updated',
    },
    404: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Blog Post not found',
    },
    422: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Validation error',
    },
  },
})

export const adminDeleteBlogPostRoute = createRoute({
  method: 'delete',
  path: '/admin/api/blog-post/{id}',
  tags: ["Blog Post"],
  request: {
    params: z.object({ id: z.string().uuid() }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: z.object({ ok: z.literal(true) }) } },
      description: 'Blog Post deleted',
    },
    404: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Blog Post not found',
    },
  },
})

export const getExamplePageListRoute = createRoute({
  method: 'get',
  path: '/api/example-page',
  tags: ["Example Page"],
  request: {
    query: z.object({
    page: z.coerce.number().int().min(1).default(1),
    per_page: z.coerce.number().int().min(1).max(100).default(10),
    include: z.string().optional().describe('Comma-separated relation field names to expand'),
    sort_by: z.enum(['title', 'created_at', 'updated_at']).optional().default('created_at'),
    sort_order: z.enum(['asc', 'desc']).optional().default('asc'),
  }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: listResponseSchema(ExamplePageSchema) } },
      description: 'List of Example Page',
    },
    400: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Invalid query parameters',
    },
  },
})

export const getExamplePageRoute = createRoute({
  method: 'get',
  path: '/api/example-page/{slug}',
  tags: ["Example Page"],
  request: {
    params: z.object({ slug: z.string() }),
    query: z.object({ include: z.string().optional().describe('Comma-separated relation field names to expand') }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: itemResponseSchema(ExamplePageSchema) } },
      description: 'Example Page',
    },
    404: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Example Page not found',
    },
  },
})

export const adminGetExamplePageListRoute = createRoute({
  method: 'get',
  path: '/admin/api/example-page',
  tags: ["Example Page"],
  request: {
    query: z.object({
    page: z.coerce.number().int().min(1).default(1),
    per_page: z.coerce.number().int().min(1).max(100).default(10),
    include: z.string().optional().describe('Comma-separated relation field names to expand'),
    sort_by: z.enum(['title', 'created_at', 'updated_at']).optional().default('created_at'),
    sort_order: z.enum(['asc', 'desc']).optional().default('asc'),
    published: z.enum(['true', 'false']).optional().describe('Filter by published state'),
  }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: listResponseSchema(ExamplePageSchema) } },
      description: 'List of Example Page',
    },
    400: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Invalid query parameters',
    },
  },
})

export const adminGetExamplePageRoute = createRoute({
  method: 'get',
  path: '/admin/api/example-page/{id}',
  tags: ["Example Page"],
  request: {
    params: z.object({ id: z.string().uuid() }),
    query: z.object({ include: z.string().optional().describe('Comma-separated relation field names to expand') }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: itemResponseSchema(ExamplePageSchema) } },
      description: 'Example Page',
    },
    404: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Example Page not found',
    },
  },
})

export const adminCreateExamplePageRoute = createRoute({
  method: 'post',
  path: '/admin/api/example-page',
  tags: ["Example Page"],
  request: {
    body: {
      content: { 'application/json': { schema: ExamplePageSchema } },
    },
  },
  responses: {
    201: {
      content: { 'application/json': { schema: itemResponseSchema(ExamplePageSchema) } },
      description: 'Example Page created',
    },
    422: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Validation error',
    },
  },
})

export const adminUpdateExamplePageRoute = createRoute({
  method: 'patch',
  path: '/admin/api/example-page/{id}',
  tags: ["Example Page"],
  request: {
    params: z.object({ id: z.string().uuid() }),
    body: {
      content: { 'application/json': { schema: ExamplePageSchema.partial().extend({ published: z.boolean().optional() }) } },
    },
  },
  responses: {
    200: {
      content: { 'application/json': { schema: itemResponseSchema(ExamplePageSchema) } },
      description: 'Example Page updated',
    },
    404: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Example Page not found',
    },
    422: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Validation error',
    },
  },
})

export const adminDeleteExamplePageRoute = createRoute({
  method: 'delete',
  path: '/admin/api/example-page/{id}',
  tags: ["Example Page"],
  request: {
    params: z.object({ id: z.string().uuid() }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: z.object({ ok: z.literal(true) }) } },
      description: 'Example Page deleted',
    },
    404: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Example Page not found',
    },
  },
})

// ─── Taxonomy routes ────────────────────────────────────────────────────────

export const getDailyPostListRoute = createRoute({
  method: 'get',
  path: '/api/taxonomy/daily-post',
  tags: ["Daily Post"],
  request: {
    query: z.object({
    page: z.coerce.number().int().min(1).default(1),
    per_page: z.coerce.number().int().min(1).max(100).default(10),
    include: z.string().optional().describe('Comma-separated relation field names to expand'),
    sort_by: z.enum(['title', 'created_at', 'updated_at']).optional().default('created_at'),
    sort_order: z.enum(['asc', 'desc']).optional().default('asc'),
  }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: listResponseSchema(DailyPostSchema) } },
      description: 'List of Daily Post',
    },
    400: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Invalid query parameters',
    },
  },
})

export const getDailyPostRoute = createRoute({
  method: 'get',
  path: '/api/taxonomy/daily-post/{id}',
  tags: ["Daily Post"],
  request: {
    params: z.object({ id: z.string().uuid() }),
    query: z.object({ include: z.string().optional().describe('Comma-separated relation field names to expand') }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: itemResponseSchema(DailyPostSchema) } },
      description: 'Daily Post',
    },
    404: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Daily Post not found',
    },
  },
})

export const adminGetDailyPostListRoute = createRoute({
  method: 'get',
  path: '/admin/api/taxonomy/daily-post',
  tags: ["Daily Post"],
  request: {
    query: z.object({
    page: z.coerce.number().int().min(1).default(1),
    per_page: z.coerce.number().int().min(1).max(100).default(10),
    include: z.string().optional().describe('Comma-separated relation field names to expand'),
    sort_by: z.enum(['title', 'created_at', 'updated_at']).optional().default('created_at'),
    sort_order: z.enum(['asc', 'desc']).optional().default('asc'),
    published: z.enum(['true', 'false']).optional().describe('Filter by published state'),
  }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: listResponseSchema(DailyPostSchema) } },
      description: 'List of Daily Post',
    },
    400: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Invalid query parameters',
    },
  },
})

export const adminGetDailyPostRoute = createRoute({
  method: 'get',
  path: '/admin/api/taxonomy/daily-post/{id}',
  tags: ["Daily Post"],
  request: {
    params: z.object({ id: z.string().uuid() }),
    query: z.object({ include: z.string().optional().describe('Comma-separated relation field names to expand') }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: itemResponseSchema(DailyPostSchema) } },
      description: 'Daily Post',
    },
    404: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Daily Post not found',
    },
  },
})

export const adminCreateDailyPostRoute = createRoute({
  method: 'post',
  path: '/admin/api/taxonomy/daily-post',
  tags: ["Daily Post"],
  request: {
    body: {
      content: { 'application/json': { schema: DailyPostSchema } },
    },
  },
  responses: {
    201: {
      content: { 'application/json': { schema: itemResponseSchema(DailyPostSchema) } },
      description: 'Daily Post created',
    },
    422: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Validation error',
    },
  },
})

export const adminUpdateDailyPostRoute = createRoute({
  method: 'patch',
  path: '/admin/api/taxonomy/daily-post/{id}',
  tags: ["Daily Post"],
  request: {
    params: z.object({ id: z.string().uuid() }),
    body: {
      content: { 'application/json': { schema: DailyPostSchema.partial().extend({ published: z.boolean().optional() }) } },
    },
  },
  responses: {
    200: {
      content: { 'application/json': { schema: itemResponseSchema(DailyPostSchema) } },
      description: 'Daily Post updated',
    },
    404: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Daily Post not found',
    },
    422: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Validation error',
    },
  },
})

export const adminDeleteDailyPostRoute = createRoute({
  method: 'delete',
  path: '/admin/api/taxonomy/daily-post/{id}',
  tags: ["Daily Post"],
  request: {
    params: z.object({ id: z.string().uuid() }),
  },
  responses: {
    200: {
      content: { 'application/json': { schema: z.object({ ok: z.literal(true) }) } },
      description: 'Daily Post deleted',
    },
    404: {
      content: { 'application/json': { schema: ErrorResponseSchema } },
      description: 'Daily Post not found',
    },
  },
})
